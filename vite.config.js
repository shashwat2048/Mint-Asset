import { defineConfig } from "vite";

export default defineConfig({
  // If you deploy to GitHub Pages under a repo subpath, set base to '/REPO_NAME/'.
  // For relative asset paths (works for many static hosts), keep './'.
  base: "./",
});


